import { createTheme } from '@mui/material/styles';
import { grey, primary, secondary, text } from './Colors';

const theme = createTheme({
  palette: {
    grey,
    primary,
    secondary,
    text,
  },
});

export default theme;